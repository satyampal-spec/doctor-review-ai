# ClinicReview AI — Environment Variables
#
# No API key required.
# Review generation is 100% client-side — no external services, no cost.
#
# Optional: override the base URL if self-hosting on a custom domain
# NEXT_PUBLIC_BASE_URL=https://your-domain.com
